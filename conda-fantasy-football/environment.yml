name: cpr-fantasy-football
channels:
  - conda-forge
  - defaults
dependencies:
  # Python version - pinned for security and stability
  - python=3.11.*

  # Core data processing
  - pandas>=2.0.0,<3.0.0

  # HTTP requests - pinned to secure version range
  - requests>=2.31.0,<3.0.0

  # Web framework - using conda-forge for better dependency resolution
  - streamlit>=1.31.0,<2.0.0

  # Visualization
  - altair>=5.0.0,<6.0.0
